/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/WillowCare",
    output: 'export',
    images: {
        domains: ['lh3.googleusercontent.com'],
      }
};

export default nextConfig;
